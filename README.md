# Vizva Dashboard Live

Static deployment package for the Vizva dashboard.

## Local

Open `index.html` directly or serve the folder with a local HTTP server.

## Deploy

This folder is ready for GitHub and Vercel deployment as a static site.
